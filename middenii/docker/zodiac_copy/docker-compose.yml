version: '3'
services:
  zodiac_copy-frontend:
    build:
      context: ./react
      dockerfile: Dockerfile
    tty: true
    volumes:
      - ./react:/var/www/app
    working_dir: /var/www/app
    command: sh -c "npx webpack && npx webpack serve && /bin/sh"
    expose:
      - "3000"
    networks:
      - default
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.zodiac_copy-frontend.rule=Host(`works.middenii.com`)  && PathPrefix(`/zodiac-copy-web`)"
      # - "traefik.http.routers.frontend.rule=Host(`works.middenii.com`)"
      - "traefik.http.routers.zodiac_copy-frontend.tls=true" #sets the service to use 
      - "traefik.http.routers.zodiac_copy-frontend.entrypoints=websecure"
      - "traefik.http.routers.zodiac_copy-frontend.tls.certresolver=myhttpchallenge"
      - "traefik.http.routers.zodiac_copy-frontend.tls.domains[0].main=works.middenii.com" #optionally restrict domain
      - "traefik.http.routers.zodiac_copy-frontend.middlewares=zodiac_copy-frontend-stripprefix"
      - "traefik.http.middlewares.zodiac_copy-frontend-stripprefix.stripprefix.prefixes=/zodiac-copy-web"
      - "traefik.http.middlewares.zodiac_copy-frontend-stripprefix.stripprefix.forceslash=true"


  zodiac_copy-backend:
    build:
      context: ./fastapi
      dockerfile: Dockerfile
    volumes:
      - ./fastapi/.dockervenv:/src/.venv
      - ./fastapi:/src
    expose:
      - "8000"  # ホストマシンのポート8000を、docker内のポート8000に接続する
    networks:
      - default
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.zodiac_copy-backend.rule=Host(`works.middenii.com`)  && PathPrefix(`/zodiac-copy-api`)"
      # - "traefik.http.routers.backend.rule=Host(`works.middenii.com`)"
      - "traefik.http.routers.zodiac_copy-backend.tls=true" #sets the service to use 
      - "traefik.http.routers.zodiac_copy-backend.entrypoints=websecure"
      - "traefik.http.routers.zodiac_copy-backend.tls.certresolver=myhttpchallenge"
      - "traefik.http.routers.zodiac_copy-backend.tls.domains[0].main=works.middenii.com" #optionally restrict domain
      - "traefik.http.routers.zodiac_copy-backend.middlewares=zodiac_copy-backend-stripprefix"
      - "traefik.http.middlewares.zodiac_copy-backend-stripprefix.stripprefix.prefixes=/zodiac-copy-api"
      - "traefik.http.middlewares.zodiac_copy-backend-stripprefix.stripprefix.forceslash=true"

networks:
  web:
    external:
      name: web


      