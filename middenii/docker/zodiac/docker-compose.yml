version: '3'
services:
  frontend:
    build:
      context: ./react
      dockerfile: Dockerfile
    tty: true
    volumes:
      - ./react:/var/www/app
    working_dir: /var/www/app
    command: sh -c "npx webpack && npx webpack serve && /bin/sh"
    expose:
      - "3000"
    networks:
      - default
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend-frontend.rule=Host(`works.middenii.com`)  && PathPrefix(`/zodiac-web`)"
      # - "traefik.http.routers.frontend.rule=Host(`works.middenii.com`)"
      - "traefik.http.routers.backend-frontend.tls=true" #sets the service to use 
      - "traefik.http.routers.backend-frontend.entrypoints=websecure"
      - "traefik.http.routers.backend-frontend.tls.certresolver=myhttpchallenge"
      - "traefik.http.routers.backend-frontend.tls.domains[0].main=works.middenii.com" #optionally restrict domain
      - "traefik.http.routers.backend-frontend.middlewares=backend-frontend-stripprefix"
      - "traefik.http.middlewares.backend-frontend-stripprefix.stripprefix.prefixes=/zodiac-web"
      - "traefik.http.middlewares.backend-frontend-stripprefix.stripprefix.forceslash=true"


  backend:
    build:
      context: ./fastapi
      dockerfile: Dockerfile
    volumes:
      - ./fastapi/.dockervenv:/src/.venv
      - ./fastapi:/src
    expose:
      - "8000"  # ホストマシンのポート8000を、docker内のポート8000に接続する
    networks:
      - default
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.zodiac-backend.rule=Host(`works.middenii.com`)  && PathPrefix(`/zodiac-api`)"
      # - "traefik.http.routers.backend.rule=Host(`works.middenii.com`)"
      - "traefik.http.routers.zodiac-backend.tls=true" #sets the service to use 
      - "traefik.http.routers.zodiac-backend.entrypoints=websecure"
      - "traefik.http.routers.zodiac-backend.tls.certresolver=myhttpchallenge"
      - "traefik.http.routers.zodiac-backend.tls.domains[0].main=works.middenii.com" #optionally restrict domain
      - "traefik.http.routers.zodiac-backend.middlewares=zodiac-backend-backend-stripprefix"
      - "traefik.http.middlewares.zodiac-backend-stripprefix.stripprefix.prefixes=/zodiac-api"
      - "traefik.http.middlewares.zodiac-backend-stripprefix.stripprefix.forceslash=true"

networks:
  web:
    external:
      name: web


      